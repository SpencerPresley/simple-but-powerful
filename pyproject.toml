[project]
name = "claude-md-sibling-discovery"
version = "0.2.0"
description = "Discovers CLAUDE.md files in sibling/cousin directories for Claude Code"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
